apiVersion: batch/v1
kind: Job
metadata:
  name: hello-migrate-7a42d7403fb1
  namespace: bitcert-attest
  labels:
    bee.dev/module: hello
    bee.dev/mighash: 7a42d7403fb1
  annotations:
    argocd.argoproj.io/sync-wave: '-100'
    argocd.argoproj.io/sync-options: Replace=true,Force=true
spec:
  backoffLimit: 6
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: flyway
        image: flyway/flyway:10-alpine
        args:
        - -url=jdbc:postgresql://postgres.bee-substrate.svc.cluster.local:5432/bee
        - -user=bee
        - -password=bee-demo
        - -connectRetries=30
        - -schemas=attest,public
        - -table=flyway_history_hello
        - -locations=filesystem:/sql
        - -baselineOnMigrate=true
        - migrate
        volumeMounts:
        - name: sql
          mountPath: /sql
      volumes:
      - name: sql
        configMap:
          name: hello-migrations
