apiVersion: batch/v1
kind: Job
metadata:
  name: batching-migrate-259abecc4ce2
  namespace: bitcert-attest
  labels:
    bee.dev/module: batching
    bee.dev/mighash: 259abecc4ce2
  annotations:
    argocd.argoproj.io/sync-wave: '-100'
spec:
  backoffLimit: 6
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: flyway
        image: flyway/flyway:10-alpine
        args:
        - -url=jdbc:postgresql://postgres.bee-substrate.svc.cluster.local:5432/bee
        - -user=bee
        - -password=bee-demo
        - -connectRetries=30
        - -table=flyway_history_batching
        - -locations=filesystem:/sql
        - -baselineOnMigrate=true
        - migrate
        volumeMounts:
        - name: sql
          mountPath: /sql
      volumes:
      - name: sql
        configMap:
          name: batching-migrations
