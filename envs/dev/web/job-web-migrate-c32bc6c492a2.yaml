apiVersion: batch/v1
kind: Job
metadata:
  name: web-migrate-c32bc6c492a2
  namespace: bitcert-attest
  labels:
    bee.dev/module: web
    bee.dev/mighash: c32bc6c492a2
  annotations:
    argocd.argoproj.io/sync-wave: '-99'
spec:
  backoffLimit: 6
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: flyway
        image: flyway/flyway:10-alpine
        args:
        - -url=jdbc:postgresql://postgres.bee-substrate.svc.cluster.local:5432/bee
        - -user=bee
        - -password=bee-demo
        - -connectRetries=30
        - -table=flyway_history_web
        - -locations=filesystem:/grants
        - -baselineOnMigrate=true
        - migrate
        volumeMounts:
        - name: grants
          mountPath: /grants
      volumes:
      - name: grants
        configMap:
          name: web-grants
