apiVersion: batch/v1
kind: Job
metadata:
  name: web-migrate-d6393e196b32
  namespace: bitcert-attest
  labels:
    bee.dev/module: web
    bee.dev/mighash: d6393e196b32
  annotations:
    argocd.argoproj.io/sync-wave: '-99'
    argocd.argoproj.io/sync-options: Replace=true,Force=true
spec:
  backoffLimit: 6
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: flyway
        image: flyway/flyway:10-alpine
        args:
        - -url=jdbc:postgresql://postgres.bee-substrate.svc.cluster.local:5432/bee
        - -user=bee
        - -password=bee-demo
        - -connectRetries=30
        - -schemas=public
        - -table=flyway_history_web
        - -locations=filesystem:/grants
        - -baselineOnMigrate=true
        - migrate
        volumeMounts:
        - name: grants
          mountPath: /grants
      volumes:
      - name: grants
        configMap:
          name: web-grants
